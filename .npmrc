# Electron + native modules (node-pty, better-sqlite3) are happiest with a
# flattened node_modules. pnpm's default symlinked layout confuses electron-builder
# and @electron/rebuild, so we hoist.
node-linker=hoisted
strict-peer-dependencies=false
